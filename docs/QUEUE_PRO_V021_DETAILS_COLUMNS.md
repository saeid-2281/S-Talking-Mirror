# Queue Pro v0.21 — Details and Column Layout

The selected-row inspector is now owned by `QueueDetailsPane`, while MainWindow
keeps temporary aliases for compatibility. The queue column controller now
persists complete header state: widths, visual order and visibility.
