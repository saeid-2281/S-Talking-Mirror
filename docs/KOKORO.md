# Kokoro Local

Kokoro is treated as an optional local provider. The adapter checks for the installed runtime and should derive voices, models, devices, and supported languages from that runtime.

Do not advertise Danish or any language unless the installed model/runtime reports support for it.
