# Google Cloud Text-to-Speech

Google Cloud Text-to-Speech uses Application Default Credentials or a safe credential reference. Raw service-account JSON and private keys must never be copied into project files, reports, or diagnostics.

The optional adapter reports dependency/setup readiness and is prepared for voice listing, language filtering, audio encoding, speaking rate, pitch, volume gain, and effects-profile support.
