# Amazon Polly

Amazon Polly uses standard AWS credential resolution such as environment/shared AWS profile plus optional region metadata. S Talking does not store AWS access keys in project files.

The optional adapter is prepared for voice listing, language filtering, engine compatibility, request IDs, and normalized AWS errors.
